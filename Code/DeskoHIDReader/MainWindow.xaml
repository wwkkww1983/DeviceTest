<Window x:Class="DeskoHIDReader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow"
        Width="1024"
        Height="300"
        WindowStartupLocation="Manual">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontSize="14"
                   FontWeight="Bold"
                   Text="{Binding DataBuffer}" />

        <TextBlock Grid.Row="1"
                   FontSize="20"
                   FontWeight="Bold"
                   Text="{Binding QRCode}" />
    </Grid>
</Window>
